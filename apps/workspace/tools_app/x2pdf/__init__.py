"""X2PDF: best-effort conversion of any file or URL to PDF.

Leaf-first note: document conversion is generic and belongs in a package
(alongside the PDF extractor); it lives in the hub Tools app for now because
that is where Tools currently ships.
"""
