name: CodeQL Security Analysis

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    - cron: '0 6 * * 1'  # Weekly on Monday at 6 AM

jobs:
  analyze:
    name: Analyze Code
    # VERIFICATION, not an assumption. I claimed earlier that codeql "needs
    # its toolcache" and had NOT measured it — this job is how that gets
    # settled. CodeQL for python/javascript is interpreted (autobuild is a
    # no-op for both), the action downloads its own bundle, and nothing here
    # needs root, so it SHOULD run on the unprivileged pool. If it does not,
    # revert this hunk and exempt with the measured reason.
    # This is currently a GREEN check, so the risk is real and accepted
    # deliberately: an exemption I cannot justify by measurement is just a
    # skip_rules entry with better manners.
    runs-on: [self-hosted, Linux, X64, spartan-cpu]
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'python', 'javascript' ]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # CodeQL's JS/TS extractor SHELLS OUT to `node` and does not ship one.
      # On GitHub-hosted runners node is preinstalled, so nothing here ever
      # had to declare it. Measured on spartan (run 30335621068): the python
      # leg passed, the javascript leg died in
      #   TypeScriptParser.verifyNodeInstallation
      #   -> "Please install Node.js and ensure 'node' is on the PATH."
      # This is a FIX, not grounds for an exemption: the dependency was
      # always there, the old runner just supplied it invisibly. vitest
      # already proved setup-node works on this pool.
      - name: Set up Node.js (JS/TS extractor requires it)
        if: matrix.language == 'javascript'
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # NO `packs:` HERE ON PURPOSE — measured, not assumed. See #558.
      #
      # The MaD barrier pack under .github/codeql/extensions/ is NOT auto-detected;
      # that claim cost two weeks. But `packs:` is NOT the way to load a LOCAL one:
      # it resolves through the container registry. Adding
      #   packs: scitex-hub/python-security-barriers
      # failed the python leg outright at `database init`:
      #   A fatal error occurred: 'scitex-hub/python-security-barriers' not found
      #   in the registry 'https://ghcr.io/v2/'.
      # A root codeql-workspace.yml does NOT change that — the workspace mechanism
      # serves the CLI's own pack resolution, not this input.
      #
      # So loading an in-repo pack needs publishing it to ghcr. (`--additional-packs`
      # was floated as the alternative; it is NOT reachable — init has 23 inputs and
      # none takes a local filesystem path. `queries:` does accept in-repo paths, but
      # that loads QUERIES; a data extension is only consumed when its PACK loads.)
      #
      # STATUS: publishing is now wired in .github/workflows/codeql-pack-publish.yml,
      # which runs on push to develop/main (never on a PR — publishing to the org
      # namespace should not be a side effect of opening one).
      #
      # DO NOT re-add `packs:` until that workflow has actually published and the
      # package is visible under the scitex-ai org. Adding it against an unpublished
      # pack fails the python leg outright at `database init`. Once published, the
      # line to add is:
      #   packs: scitex-ai/python-security-barriers
      # Note the scope is the ORG (scitex-ai), not the repo (scitex-hub) — the
      # original attempt used the repo name, which was never a resolvable ghcr
      # coordinate. Delete this paragraph when the line lands.
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          queries: security-and-quality

      - name: Autobuild
        uses: github/codeql-action/autobuild@v3

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{matrix.language}}"
