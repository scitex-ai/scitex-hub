# Pre-commit hooks for security and code quality
# Install: pip install pre-commit && pre-commit install
# Run manually: pre-commit run --all-files

repos:
  # Detect secrets
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.5.0
    hooks:
      - id: detect-secrets
        args: ['--baseline', '.secrets.baseline']
        exclude: package-lock.json

  # General checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: detect-private-key
      - id: check-added-large-files
        args: ['--maxkb=1000']
      - id: check-merge-conflict
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json

  # Python security
  - repo: https://github.com/PyCQA/bandit
    rev: 1.8.6
    hooks:
      - id: bandit
        args: ['-r', 'apps/', 'config/', '-lll', '--exit-zero']
        pass_filenames: false
        files: \.py$
        exclude: migrations/

  # Python formatting (optional)
  - repo: https://github.com/psf/black
    rev: 25.9.0
    hooks:
      - id: black
        language_version: python3.11

  # Incremental testing (disabled: requires Docker for DB access)
  # Run inside Docker instead: make test-testmon
  # - repo: local
  #   hooks:
  #     - id: pytest-testmon
  #       name: pytest-testmon
  #       entry: python -m pytest --testmon -x -q --tb=short
  #       language: system
  #       types: [python]
  #       pass_filenames: false
  #       stages: [pre-commit]
