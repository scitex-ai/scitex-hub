# Coverage reporting configuration for scitex-hub.
#
# Hard 90% project + patch floors so the bar is visible (PS-161).
coverage:
  status:
    project:
      default:
        target: 90%
        threshold: 1%
    patch:
      default:
        target: 90%
        threshold: 1%

comment: false
